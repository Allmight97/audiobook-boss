[package]
name = "abb-media-core"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
specta = { version = "=2.0.0-rc.25", features = ["derive"] }
sha2 = "0.11.0"

[lints]
workspace = true
