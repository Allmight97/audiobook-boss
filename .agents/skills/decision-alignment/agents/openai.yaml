interface:
  display_name: "Decision Alignment"
  short_description: "Align ABB decisions and doc routing"
  default_prompt: "Use $decision-alignment to align this ABB decision against repo truth before implementation."
