# Issue #206 Migration Handoff: Execution Briefing for Codex

**Date**: 2026-02-10
**Updated**: 2026-02-11 (post-Phase 6 sync)
**Session**: Solo migration execution (human owner + Codex/agents)
**Purpose**: Keep migration execution aligned to ground truth while implementation is in flight.

---

## Architecture Snapshot (Current State)

### Frontend: Vanilla TypeScript + Imperative DOM

**No framework exists.** The UI is hand-written TypeScript with imperative DOM manipulation:

- **Structure**: Single monolithic `index.html` (961 LOC) defines all UI structure as static HTML
- **Logic**: TypeScript modules in `src/ui/` grab elements via `document.getElementById()` / `querySelector()`
- **Wiring**: Each module exports an `init*()` function called from `src/main.ts` on `DOMContentLoaded`
- **State**: Module-level `let` variables + manual DOM sync (`.textContent`, `.classList`, `.style`)
- **Events**: Manual listener attachment in `init*()` functions

**Key implication**: Migration is not "swap out framework X for framework Y" — it's introducing the *first* component framework and converting imperative DOM code to declarative Svelte components.

### Styling: Tailwind v4 + Custom CSS (Hybrid)

**Tailwind v4 is already installed and wired**:
- PostCSS config: `postcss.config.js` uses `@tailwindcss/postcss`
- Tailwind config: `tailwind.config.js` scans `index.html` + `src/**/*.{js,ts}`
- Entry point: `src/styles.css` starts with `@import "tailwindcss";`

**Current usage**:
- ~1,600 lines of hand-written CSS in `src/styles.css` using CSS custom properties for theming (`--bg-main`, `--text-primary`, etc.)
- Tailwind utilities used sparingly in `index.html` (`flex`, `gap-2`, `text-xs`, `mb-2`) alongside custom classes
- Some hand-rolled utility classes duplicate Tailwind (`.grid-cols-2`, `.text-xs`)

**Key implication**: "Add Tailwind" is *not* Phase 1 work — Tailwind is ready. Phase 1 is really "Add Svelte to Vite + design token system."

### IPC Layer: Three-Tier Typed Bridge

**Tier 1 — Generated bindings** (`src/lib/generated/tauri.ts`, 473 LOC):
- Auto-generated by tauri-specta via `bun run bindings:generate`
- 17 commands, 2 events, all Rust types exported as TS
- **Never hand-edited** (drift check in `scripts/checks.sh standard`)

**Tier 2 — Bridge adapter** (`src/lib/bridge.ts`, 363 LOC):
- Wraps generated bindings with stable command/event normalization
- Normalizes between Generated types (uses `null`) and Legacy types (uses `undefined`)
- Provides typed `bridge.*` helpers plus `bridge.listen()`, `bridge.open()`, `bridge.openExternal()`
- Falls back to mocks in dev when Tauri unavailable
- Legacy migration wrapper path was retired during migration execution (issue #203 closure evidence in migration PR)

**Tier 3 — UI callsites**: Components call typed `bridge.*` helpers directly

**Key implication**: Bridge layer remains framework-agnostic. Svelte components import and call `bridge.*` directly — no additional adapters needed.

### Build Tooling

- **Vite**: Minimal config (`vite.config.ts`), no framework plugins yet
- **TypeScript**: Strict mode, ES2020 target, bundler resolution
- **PostCSS**: Only `@tailwindcss/postcss`
- **Tailwind**: Content scan includes `index.html` + `src/**/*.{js,ts}` — will need `*.svelte` added

---

## Component Complexity Map

| Component | Files | Total LOC | Risk | Notes |
|-----------|-------|-----------|------|-------|
| **StatusPanel** | `statusPanel/*.ts` + domain/services | ~1,700 | **HIGHEST** | Class-based singleton. Parallel batch job tracking, rAF coalescing, progress throttling. Migrate *last*. |
| **FileList** | `fileList/*.ts` | ~1,140 | **HIGH** | Multi-select, drag reorder, sort, metadata sync, state sharing. Migrate near end. |
| **OutputPanel** | `outputPanel/*.ts` | ~665 | **MEDIUM** | Directory selection, ABS path preview, naming config, estimated size. |
| **MetadataLookup** | `metadataLookup.ts` | 581 | **MEDIUM-HIGH** | Modal with search, queue workflow, cover art integration, heavy DOM rendering in JS. |
| **EncoderPanel** | `encoderPanel/*.ts` | ~525 | **MEDIUM** | Encoder selection, bitrate/quality modes, availability checks, feature flags. |
| **MetadataForm** | `metadataForm.ts` | 444 | **MEDIUM** | Form read/write, dirty tracking, multi-select mode, mixed placeholders. |
| **CoverArt** | `coverArt.ts` | 435 | **MEDIUM** | Drag-drop, URL loading, MIME detection, clipboard paste, global state on `window`. |
| **FileImport** | `fileImport.ts` | 177 | **LOW** | Drag-drop handler, click-to-select, file filtering. Good early candidate. |
| **JobControls** | `jobControls.ts` | 155 | **LOW** | Merge toggle, max concurrent selector, localStorage persistence. Good early candidate. |
| **TagPreview** | `tagPreview.ts` | 112 | **LOWEST** | Pure display component. Reads input values, renders tag grid. No bridge calls, no state management. **Phase 3 spike complete.** |

**Migration order guidance**: Phase 3 spike is complete (TagPreview). Phase 4 panel migration sequence is complete in the active branch, including FileList and StatusPanel endgame waves, using the rolling per-panel outcome-test gate.

---

## Issue Coherence Findings

### Dependency Graph Status

- **#193 (typed IPC foundation)**: **CLOSED** — Phase 1 is complete, contracts are stable, drift checks are gated
- **#203 (bridge migration wrapper sunset)**: migration branch implementation complete; issue closure is tied to merge
- **#206 (this epic)**: **OPEN** — needs corrections (see below)

### Where #206 Misleads

1. **#206 says "#203 must be resolved first"** — but #203's own sunset condition says "remove by framework migration Phase 2 completion, no later than 2026-05-31." The spec doc (`docs/specs/frontend-framework-evaluation.md`, line 987) agrees. The bridge wrapper *exists to support the transition* — it doesn't need to die before migration starts. Spike can use new typed bindings directly while old panels still use bridge. **Fix**: Soften #206 language to "should be completed during migration."

2. **#206's Phase 1 says "Tailwind + token foundation"** — but Tailwind v4 is already installed and wired (PostCSS, config, utilities in HTML). Phase 1's *real* work is Svelte introduction into Vite + design token system. **Fix**: Clarify Phase 1 scope.

3. **Issue linkage updates**:
   - **Design checkpoint** (spec Phase 2): baseline screenshots, token centralization, `docs/design-system.md` — no issue exists
   - StatusPanel re-export shim + FileList mutable state export fallback were tracked under #210 and removed in Phase 5 execution
   - **Pre-migration perf baseline** capture (spec Phase 2, step 6) — no issue exists

4. **Release impact**: **Parallel/non-blocking.** Migration is active now; keep `docs/RELEASE_CHECKLIST.md` running as the release lane while migration work proceeds.
5. **Visual regression tooling**: currently non-gating by execution decision in solo/macOS mode; revisit when multi-platform rollout becomes active.

### Authoritative Source of Truth

**`docs/specs/frontend-framework-evaluation.md`** (1,200+ LOC) is the canonical spec, *not* the GitHub issue. The spec recommends:

- **Phase 1**: Full-stack type safety (done, #193)
- **Phase 2**: Design checkpoint (tokens, variants, baseline screenshots, design system doc)
- **Phase 3**: Framework migration spike (one small panel to Svelte 5) — **complete (TagPreview)**
- **Phase 4**: Panel-by-panel migration (JobControls → StatusPanel, 10 panels total) — **complete in migration branch**
- **Phase 5**: High-complexity endgame + fallback burn-down — **complete in migration branch**
- **Phase 6**: Cleanup (remove legacy styles/modules/wiring) — **complete in migration branch**

The spec chooses **Svelte 5** for: lowest migration cost (compiles to imperative DOM like current code), best co-location (`.svelte` SFCs), smallest bundle (~28KB), 1:1 state pattern mapping (`let foo` → `let foo = $state(...)`).

---

## TypeScript Return Type Audit Summary

**Audit date**: 2026-02-10 (agent a9c068b)
**Scope**: All TS files in `src/` (68 files, excludes generated code)

| Category | Found | Unnecessary | Necessary |
|----------|-------|-------------|-----------|
| Explicit function return types | ~270 | ~170-200 | ~70-100 (exports, type guards) |
| Type assertions (`as`) | ~165 | ~10-15 | ~150 (DOM casts, boundary code) |
| Redundant `let` annotations | ~35 | ~15-20 | ~15-20 (`\| null` unions) |
| **TOTAL** | ~470 | **~195-235** | ~235-270 |

**The debate**: Explicit return types on internal/private functions create maintenance burden without safety gain — TS inference already catches drift at callsites. Explicit returns have value at *exported API boundaries* (prevent accidental contract changes) and are *required* for type guards (`is` predicates).

**New convention** (being added to `src/AGENTS.md`):
> "Prefer inference for return types. Explicit return types are encouraged at exported API boundaries and required for type guards (`is` predicates). Omit elsewhere."

**Cleanup scope**:
- Priority 1: Remove `: void` on ~80 private functions (~10-15 files) — mechanical, low-risk
- Priority 2: Remove trivial returns on private fns (~40)
- Priority 3: Remove redundant `let` annotations (~15-20)
- Priority 4: Replace `as HTMLElement` with `instanceof` (~10-15)

**Action**: Standalone GH issue created (not under #206 — repo-wide concern, not migration-specific). Tag `tech-debt`, `frontend`. No milestone.

---

## What Codex Should Do Next

See **Section 3 of the plan file** (`/Users/jstar/.claude/plans/vectorized-wobbling-hare.md`) for the full Codex prompt. Summary:

**Mission**: Produce a high-level implementation plan for #206 with phase breakdown, sequencing rationale, key architectural decisions, risk areas, and fallback integration strategy.

**Read first**:
- `docs/specs/frontend-framework-evaluation.md` — authoritative spec (not the GH issue!)
- `docs/specs/full-stack-typesafety-handoff.md` — Phase 1 completion criteria
- `docs/engineering/fallback-register.md` — search "migration" for bound fallbacks
- `CLAUDE.md`, `src/AGENTS.md`, `src-tauri/AGENTS.md` — repo conventions

**Then inspect**:
- `index.html`, `src/main.ts` — current UI structure/wiring
- `src/lib/bridge.ts`, `src/lib/generated/tauri.ts` — IPC layer
- `src/ui/` — sample a few components to understand imperative DOM pattern
- `vite.config.ts`, `tailwind.config.js`, `package.json` — build tooling

**Produce**: Phase breakdown + key decisions + risk areas — not file-by-file spec. Apply full engineering rigor, no shortcuts.

**Don't**: Assume Svelte patterns without spec/codebase research. Rush the design. Produce implementation details yet.

---

## Recommendations for Codex

1. **Start with the spec doc, not the GH issue.** #206 has inaccuracies. The spec is ground truth.

2. **Treat #203 as "complete during," then verify closure evidence.** The migration branch now removes the transitional wrapper usage path and switches callsites to typed bridge helpers.

3. **Phase 1 is really "Svelte + design tokens," not "Tailwind setup."** Tailwind infrastructure is done.

4. **Keep issue tracking aligned with implementation state**: design checkpoint work, fallback closure evidence, and perf baseline history should stay in-sync with active migration commits.

5. **Treat TagPreview as the proven Phase 3 baseline and run Phase 4 as a rolling gate.** Validate each migrated panel with outcome tests before moving on; avoid front-loading a single migration-wide test block.

6. **Bridge layer is framework-agnostic.** Svelte components call `bridge.*` directly. No new IPC adapters needed.

7. **Watch the fallback register for migration-bound entries and closure evidence.** Keep register state and migration handoff notes aligned after each closure.

---

## Session Artifacts

- Plan file: `/Users/jstar/.claude/plans/vectorized-wobbling-hare.md`
- Explore agent reports: a241bd2 (issue dependency graph), a9c068b (TS audit), a15bef4 (frontend architecture)
- This handoff doc: `docs/specs/206-migration-handoff.md` (you are here)
