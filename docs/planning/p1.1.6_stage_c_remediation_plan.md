## P1.1.6 – Stage C Remediation Plan (raise all items to ≥4)

### Goal
Bring the Stage C split of `src/ui/fileList/` up to a consistent “4” (solid, senior-ready) across structural completeness, API/contract stability, build readiness, code clarity, and type safety, while preserving behavior and public imports prior to Stage D.

### Scope and Invariants
- No functional changes to the UI behavior or Rust/TS contracts.
- Preserve import path and surface: `import { ... } from "./ui/fileList"` must remain valid.
- Maintain event contracts and types in `src/types/events.ts`.
- Keep code style (strict TS, no `any` where avoidable) and file/function size targets from the combined plan.

### Priority Legend
- P0: Blocking before Stage D
- P1: Should do before closing Stage C
- P2: Nice-to-have if time permits during Stage C, otherwise queue for a short follow-up

---

## Phase 1 — P0 blockers (Required before Stage D) ✅

Purpose: Fix the aggregator shim so imports are stable, eliminate non-deterministic module resolution, and validate a clean TypeScript build.

Tasks (atomized)
1) Aggregator shim (P0)
   - Create/replace `src/ui/fileList.ts` with a thin re-export of the folder facade:
     ```ts
     export * from './fileList/index';
     ```
   - Ensure the file is tracked in Git (no empty/untracked shadow file locally).
   - Do not add any additional side-effects here; side-effects remain in `src/ui/fileList/index.ts`.

2) Build verification (P0)
   - Run:
     ```bash
     npm run build
     bash scripts/ensure-contract.sh
     bash scripts/sg/size_budget.sh
     ```
   - Confirm `./ui/fileList` importers compile, with no missing export errors.

3) Quick manual smoke (P0)
   - Drag/drop one valid file → list renders; stats show; sort/clear buttons visible as expected.
   - Drag/drop two files → toggle sort works; selection updates; clear-all hides buttons.

Acceptance criteria
- `npm run build` passes with `moduleResolution: bundler`.
- `scripts/ensure-contract.sh` shows no contract regressions.
- All existing imports from `./ui/fileList` compile and run (no missing export/runtime errors).

**Done** - Phase 1 implemented and committed. Aggregator shim fixed; build/contract/size green. No functional changes.

---

## Phase 2 — Stabilization and quality lifts (P1/P2) ✅

Purpose: Raise code clarity and type safety to ≥4, reduce duplication, and remove dead stubs. Keep behavior unchanged.

Tasks (atomized)
1) Centralize button UI updates in `dom.ts` (P1)
   - In `actions.ts`: remove direct DOM manipulation of sort/clear buttons in `displayFileList` and elsewhere.
   - Replace with calls to `updateButtonVisibility()` and `updateSortButtonText(...)` from `dom.ts`.
   - Ensure `displayFileList`, `toggleFileSort`, and `clearAllFiles` call:
     - `updateFileListDOM()`
     - `updateButtonVisibility()`
     - `updateTotalStats()` (where totals change)

2) Remove or implement stub (P1)
   - In `src/ui/fileList/events.ts`, the `initDOMEventHandlers` stub is unused.
   - Either delete the stub (preferred) or implement it to delegate to existing logic. Avoid confusing future readers.

3) Strengthen type safety (P1)
   - In `actions.ts`:
     - Import and use `AudiobookMetadata` for `loadFileMetadata`/`populateMetadataForm`.
     - Replace `any` with `AudiobookMetadata` (optional fields allowed).
     - Type the `invoke` return: `invoke<AudiobookMetadata>('read_audio_metadata', { filePath })`.
   - Ensure there are no new `any` types in the edited files.

4) Cross‑platform file name extraction (P2)
   - Replace all instances of `path.split('/')` with `path.split(/[\\\\/]/)` in `dom.ts` and `actions.ts`.
   - Behavior unchanged; this just avoids platform assumptions during development.

5) Test harness alignment (P2)
   - In `src/main.ts` `window.testCommands`:
     - Wire `testMoveFile(index, direction)` to call `moveFileUp/Down` from `./ui/fileList`.
     - Wire `testSortFiles()` to `toggleFileSort()`.
   - Keep this minimal; purpose is to reduce confusion for manual testing.

6) Dev note for maintainers (P2)
   - Add a short `src/ui/fileList/README.md` describing responsibilities of `state.ts`, `dom.ts`, `actions.ts`, `events.ts`, and that `src/ui/fileList.ts` is the aggregator shim preserving imports.

**Done** - Phase 2 implemented and committed. Deduplicated button updates; removed unused stub; strengthened type safety. No functional changes.

Validation
- Run:
  ```bash
  npm run build
  bash scripts/ensure-contract.sh
  bash scripts/sg/size_budget.sh
  ```
- Manual smoke (unchanged UX):
  - List renders and updates after remove/move/sort.
  - Sort button text toggles via `updateSortButtonText`.
  - Clear-all hides both sort and clear buttons; re-import shows them again.
  - Size and selection indicators update consistently.

Acceptance criteria
- No duplicated button visibility/text logic in `actions.ts`.
- No unused stubs in `events.ts`.
- `actions.ts` uses `AudiobookMetadata` instead of `any` (no loss of behavior).
- File name extraction uses cross‑platform split.
- `npm run build` remains green; no changes to public imports or event contracts.

---

### How this raises ratings to ≥4
- Structural split completeness → Aggregator shim fixed (Phase 1) = 4/5.
- API/contract preservation → Stable `./ui/fileList` re-exports (Phase 1) = 4/5.
- Build readiness → Verified green build and contract check (Phase 1) = 4/5.
- Code clarity/maintainability → Deduplicated UI updates, removed stub, README (Phase 2) = 4/5.
- Type safety → Replace `any` with `AudiobookMetadata` and typed `invoke` (Phase 2) = 4/5.

### Time/cost estimate
- Phase 1 (P0): ~15–30 minutes.
- Phase 2 (P1/P2 combined): ~45–60 minutes.

### Rollback
- All changes are localized to `src/ui/fileList/*` and `src/main.ts` test harness. If any regression arises, revert the edits in a single commit.


