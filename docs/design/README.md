# Design Assets

This folder stores migration-era design artifacts.

- `baselines/`: optional screenshot baselines for visual comparison

Current policy (solo/macOS execution mode): visual regression artifacts are non-gating during active migration, then re-evaluated when multi-platform rollout starts.

